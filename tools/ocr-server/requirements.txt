# 安裝指令：pip install -r requirements.txt
# 建議 Python 3.10+，PaddleOCR v5 需要 PaddlePaddle

flask>=3.0
flask-cors>=4.0
paddlepaddle>=3.0.0       # CPU 版；GPU 版改成 paddlepaddle-gpu
paddleocr>=2.9.0
opencv-python-headless>=4.8
numpy>=1.24
